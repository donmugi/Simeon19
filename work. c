#include <stdio.h>

int main() {
    int withdraw;
    int amount =50000;
    scanf("%d", &withdraw);
    int balance = amount-withdraw;
    printf ("THe amount is:%d shillings \n", balance);

    return 0;