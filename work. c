#include <stdio.h>

int main() {
    int withdraw;
    int amount =50000;
    scanf("%d", &withdraw);
    amount = amount-withdraw;
    printf ("THe amount is:%d shillings \n", amount);

    return 0;